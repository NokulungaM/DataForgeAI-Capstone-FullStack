// import React from "react";

// const UserProfile = ({ profile}) => {
//   return (
//     <div className="bg-white shadow-lg rounded-lg p-6 flex flex-col items-center text-center">
//       <img
//         src={profile.profilePicture || "https://example.com/default-avatar.jpg"} // Default avatar if no profile picture
//         alt={profile.username || "User"} // Default alt text
//         className="w-24 h-24 rounded-full mb-4 object-cover"
//       />
//       <h2 className="text-lg font-semibold text-gray-800">
//         {profile.username || "Anonymous"} // Default username if not available
//       </h2>
//       <p className="text-sm text-gray-500 italic">
//         Mood: {profile.mood || "Too Lazy to cook"} // Default mood if not available
//       </p>
//     </div>
//   );
// };

// export default UserProfile;
